#include <iostream>
#include <string>
using namespace std;
int main()
{
    std::string s = "A=B+C-D";
    /*
	std::string delim = "+",delim1="-";

    auto int start = 0U;
    auto int end = s.find(delim);
    auto int end1 = s.find(delim1);
    while (end != std::string::npos || end1 != std::string::npos)
    {
        std::cout << s.substr(start, end - start) << std::endl;
        start = end + delim.length();
        end = s.find(delim, start);
    }

    std::cout << s.substr(start, end);
*/
 vecor
  for(int i=0;i<strlen(s);i++)
    {
	}
}
